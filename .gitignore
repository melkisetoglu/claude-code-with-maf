# .NET build artifacts
bin/
obj/
*.user
*.suo
.vs/

# IDE
.idea/
*.swp
.DS_Store

# User data — sessions contain conversation history with Claude. Never commit.
sessions/

# User data — the model writes cross-session notes here via
# FileMemoryProvider (Step 12). Personal, possibly sensitive. Opt-in by
# creating the directory — ignored once it exists.
memory/

# Log files written by Observability/FileLogger.cs (Step 5).
*.log

# Local secrets / env overrides
.env
*.env.local
